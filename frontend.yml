- name: Frontend Setup
  hosts: all
  become: true
  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest

- name: Start nginx
  ansible.builtin.systemd_service:
    state: restarted
    name: nginx
